{
    "name": "worqr",
    "version": "1.0.0",
    "description": "A distributed, reliable, atomic, work queueing system that only requires redis as the backend.",
    "desqription": "Worqr does all ur worq. I can haz worq? No... All ur worq are belong to worqr.",
    "main": "dist/index",
    "types": "dist/index",
    "scripts": {
        "build": "npm run tslint && tsc",
        "example": "ts-node -r dotenv/config ./test/example.ts",
        "prepack": "npm run build",
        "start": "nodemon",
        "test": "mocha --exit -r ts-node/register ./test/test.ts",
        "tslint": "tslint -t stylish 'src/**/*.ts' --force"
    },
    "author": "Bryan Jenks (droplit.io), Samir Mammadov (droplit.io), Davis Kauffmann (droplit.io)",
    "license": "MIT",
    "dependencies": {
        "debug": "^3.1.0",
        "redis": "^2.8.0",
        "uuid": "^3.3.2"
    },
    "devDependencies": {
        "@types/chai": "^4.1.4",
        "@types/mocha": "^5.2.5",
        "@types/node": "^9.4.6",
        "@types/redis": "^2.8.6",
        "@types/uuid": "^3.4.3",
        "chai": "^4.1.2",
        "dotenv": "^5.0.1",
        "mocha": "^5.2.0",
        "nodemon": "^1.14.12",
        "ts-node": "^6.1.0",
        "tslint": "^5.11.0",
        "typescript": "^2.9.1"
    }
}
